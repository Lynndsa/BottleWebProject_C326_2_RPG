# Алгоритмы на графах — веб-приложение на Python Bottle

> Учебный проект по дисциплине **«Алгоритмы на графах»**
> Группа **С326**, команда №2

---

## О проекте

Веб-приложение реализует три задачи из различных предметных областей, каждая из которых основана на классических алгоритмах теории графов:

| Вариант | Тематика                             | Алгоритм       |
| ------- | ------------------------------------ | -------------- |
| 1       | Эпидемиология и сетевая безопасность | BFS            |
| 2       | Транспортная логистика               | Dijkstra + TSP |
| 3       | Анализ финансовых транзакций         | DFS            |

Приложение позволяет:

* вводить граф вручную;
* загружать данные из `.txt` файлов;
* генерировать тестовые данные;
* визуализировать графы в формате SVG;
* получать результаты работы алгоритмов через веб-интерфейс.

---

# Используемые технологии

| Технология                | Назначение                 |
| ------------------------- | -------------------------- |
| Python 3.10+              | Язык программирования      |
| Bottle                    | Веб-фреймворк              |
| Graphviz / svgwrite       | Построение SVG-графов      |
| TPL / CSS / JavaScript    | Пользовательский интерфейс |
| Visual Studio / VS Code   | Среда разработки           |

---

# Установка

Установите необходимые зависимости:

```bash
pip install bottle
```

---

# Запуск проекта

Перейдите в корневую директорию проекта:

```bash
cd project
```

Запустите сервер:

```bash
python app.py
```

После запуска откройте браузер:

```text
http://localhost:8080
```

---

# Реализованные алгоритмы

## Моделирование распространения вируса (BFS)

### Постановка задачи

Имеется связный неориентированный граф:

* вершины — люди или компьютеры;
* рёбра — связи между ними.

Известна начальная заражённая вершина.

На каждом шаге инфекция распространяется на соседние вершины с заданной вероятностью передачи.

### Используемый алгоритм

**Обход графа в ширину (Breadth-First Search, BFS)**

Позволяет моделировать распространение заражения по уровням удалённости от источника.

---

## Планирование экскурсий (TSP)

### Постановка задачи

Задана дорожная сеть:

* количество вершин: `N ≤ 50`;
* имеется отель;
* задан список достопримечательностей `M ≤ 8`.

Требуется построить кратчайший маршрут:

* начинается в отеле;
* проходит через все достопримечательности;
* заканчивается в отеле.

### Используемые алгоритмы

1. **Алгоритм Дейкстры**

   * поиск кратчайших расстояний между ключевыми вершинами.

2. **Полный перебор перестановок**

   * поиск оптимального порядка посещения объектов.

---

## Анализ финансовых транзакций (DFS)

### Постановка задачи

Рассматривается ориентированный граф транзакций:

* вершины — кошельки;
* дуги — денежные переводы;
* каждая дуга содержит:

  * сумму перевода;
  * временную метку.

Необходимо обнаружить длинные цепочки последовательного перемещения средств, которые могут свидетельствовать о схемах отмывания денег.

### Используемый алгоритм

**Depth-First Search (DFS) с backtracking**

Переход по ребру выполняется только если:

```text
next_vertex ∉ visited
timestamp > previous_timestamp
```

Алгоритм ищет максимальные ациклические пути движения средств.

---

# Структура проекта

```text
project/
│
├── app.py                     # Точка входа
├── routes.py                  # Маршруты Bottle
│
├── bfs_algorithm.py           # BFS
├── tsp_algorithm.py           # TSP
├── dfs_algorithm.py           # DFS
│
├── bfs_validator.py           # Валидация BFS
├── tsp_validator.py           # Валидация TSP
├── dfs_validator.py           # Валидация DFS
│
├── views/
│   ├── layout.tpl
│   ├── index.tpl
│   ├── bfs.tpl
│   ├── tsp.tpl
│   ├── dfs.tpl
│   └── about.tpl
│
├── static/
│   ├── favicon.ico
│   │
│   └── content/
│       ├── site.css
│       ├── menu.css
│       ├── bfs.css
│       ├── excursion.css
│       └── dfs.css
│
└── README.md
```

---

# Форматы входных данных

## DFS — транзакции

```text
<отправитель> <получатель> <сумма> <время>

A1B2C3 D4E5F6 1500.00 1700000001
D4E5F6 G7H8I9 750.00 1700000120
```

---

## TSP — рёбра графа

```text
<вершина_u> <вершина_v> <вес>

1 2 5
2 3 3
1 3 10
```

---

# Результаты работы

После выполнения алгоритма пользователь получает:

* SVG-визуализацию графа;
* статистику по графу;
* найденные маршруты и цепочки;
* подсветку подозрительных путей;
* таблицу исходных данных.

Результаты отображаются непосредственно на веб-странице.

---

# Особенности запуска

По умолчанию приложение работает в режиме разработки:

```python
debug=True
reloader=True
```

Для публикации рекомендуется использовать:

```python
debug=False
reloader=False
```

Статические файлы обслуживаются маршрутом:

```text
/ static / <filepath>
```

из каталога:

```text
./static/
```

---

# Авторы

**Группа С326**

* Андрей Гуляев
* Анастасия Пономарёва
* Лада Романова

---

## Учебный проект

Курс: **Алгоритмы на графах**
Учебный год: **2025–2026**
